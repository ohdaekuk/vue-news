export default {
  // news => actions에서 넘겨준 데이터
  // SET_NEWS(state, news) {
  //   state.news = news;
  // },
  // SET_JOBS(state, jobs) {
  //   state.jobs = jobs;
  // },
  // SET_ASK(state, ask) {
  //   state.ask = ask;
  // },
  SET_USER(state, user) {
    state.user = user;
  },
  SET_ITEMS(state, items) {
    state.items = items;
  },
  SET_LIST(state, list) {
    state.list = list;
  },
};
